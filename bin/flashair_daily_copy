#!/usr/bin/env ruby

require 'bundler/setup'
require 'flashair_daily_copy'

if FlashairDailyCopy::Api.hostname_is_available?
  puts "starting copy..."
  FlashairDailyCopy::CLI.run
  puts "end copy..."
else
  puts "#{FlashairDailyCopy::Api.hostname} is not available"
end

